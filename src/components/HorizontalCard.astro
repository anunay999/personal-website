---
const { title, img, desc, url, badge, tags, target = '_blank' } = Astro.props;
---

<div class="rounded-lg  hover:shadow-xl transition ease-in-out hover:scale-[107%] hover:bg-accent delay-100 hover:text-white">
  <a href={url} target={target}>
    <div class="hero-content flex-col md:flex-row">
      {img && (
        <div class="w-[200px] h-[150px] flex items-center justify-center overflow-hidden rounded-lg shrink-0">
          <img src={img} alt={title} class="max-w-full max-h-full object-contain" />
        </div>
      )}
      <div class="grow w-full">
        <h1 class="text-xl font-bold">
          {title}
          {badge && <div class="badge badge-secondary mx-2">{badge}</div>}
        </h1>
        <p class="py-1 text-1xl">{desc}</p>
        <div class="card-actions justify-end">
          {tags && tags.map((tag) => <div class="badge badge-outline">{tag}</div>)}
        </div>
      </div>
    </div>
  </a>
</div>

