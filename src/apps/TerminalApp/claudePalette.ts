/* Palette for the in-terminal Claude Code cosplay. Kept separate from the
   global theme tokens (`T`) so the orange brand colors don't leak into other
   surfaces. */
export const CLAUDE = {
  border: "#d97757",
  accent: "#d97757",
  ink: "#f5f1ec",
  inkDim: "#bdb4ab",
  inkFaint: "#7a7269",
  err: "#ff8a8a",
};
